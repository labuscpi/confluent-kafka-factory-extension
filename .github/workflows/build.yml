name: Build & Test

on:
  push:
    branches: [ main, netcoreapp3.1 ]
  pull_request:
    branches: [ main, netcoreapp3.1 ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET Core 3.1.x
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '3.1.x'
      - name: Setup .NET Core 5.0.x
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '5.0.x'
      - name: Setup .NET Core 6.0.x
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0.x'
      - name: Restore
        run: dotnet restore Confluent.Kafka.FactoryExtension.sln --verbosity minimal --force
      - name: Build
        run: dotnet build Confluent.Kafka.FactoryExtension.sln --configuration Release --no-restore --verbosity minimal
      - name: Test
        run: dotnet test Confluent.Kafka.FactoryExtension.sln --configuration Release --no-build --verbosity minimal
